fn main() {
    use std::net::IpAddr;

    let home: IpAddr = "127.0.0.1"
        .parse()
        .expect("Hardcoded IP address should be valid");

    // The rest of the code from this section wanted me to use the Guessing game from chapter 2.
    // However I have already modified that code into my own version so it's a little late.
    // Still, the point makes sense. Refer back to this chapter when error handling.
}